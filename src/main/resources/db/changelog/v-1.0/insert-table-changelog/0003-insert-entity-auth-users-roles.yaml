databaseChangeLog:
  - changeSet:
      id: insert-row-to-auth-users-roles-table
      author: isfandiyor
      changes:
        - sql:
            sql: INSERT INTO auth_users_roles(user_id, role_id)
                VALUES (-1, -1), 
                       (-2, -1), 
                       (-3, -2)